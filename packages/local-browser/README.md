# cordis-plugin-social-browser-local

Cordis `socialBrowser` provider using an isolated persistent Microsoft Edge or
Google Chrome profile and a loopback CDP connection. No API key is required.

See https://github.com/enginefuture/cordis-social for configuration.
