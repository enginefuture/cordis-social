# cordis-plugin-social-x

X platform adapter for Cordis Social. Supports persistent interactive login,
text and image posts, replies, quote posts, and verified preview/publish flows.

Requires `social` and `socialBrowser` Cordis services.
