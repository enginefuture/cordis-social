# cordis-plugin-social

Cordis service for lifecycle-safe social operations. Provides `ctx.social`,
platform registration, authentication delegation, prepared-post TTL, and the
two-phase `preparePost()` / `publishPost()` confirmation flow.

See https://github.com/enginefuture/cordis-social for usage and policy.
